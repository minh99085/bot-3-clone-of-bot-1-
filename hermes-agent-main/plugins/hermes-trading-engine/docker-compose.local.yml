# Local laptop training overlay (Bot 3). Used by scripts/run-bot3-local-training.*
# Skips the Dockerfile pytest gate (~67 known-stale tests) so first build succeeds.
services:
  hermes-trading-engine:
    build:
      args:
        RUN_TESTS: "0"
  hermes-training:
    build:
      args:
        RUN_TESTS: "0"
