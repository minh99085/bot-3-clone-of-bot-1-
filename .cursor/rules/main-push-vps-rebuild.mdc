---
description: Git + VPS deploy workflow — main only, full rebuild always
alwaysApply: true
---

# Ship workflow (operator mandate) — Bot 3

**Do NOT create feature branches or PRs.** Work on `main` only.

Every code/config change is incomplete until this full pipeline finishes:

1. `git checkout main` (if needed) → edit → test
2. `git commit` → `git push origin main`
3. Sync VPS (`.\scripts\sync-vps-bot3.ps1` or `./scripts/sync-vps-bot3.sh`)
4. On VPS plugin dir:
   `docker compose down --remove-orphans`
   `docker compose build`
   `docker compose up -d --force-recreate --remove-orphans`
5. Verify (`.\scripts\verify-sync-bot3.ps1`)

Never `-SkipRebuild` or `docker compose restart` unless the operator explicitly requests code-only sync in the current message.

Full detail: `.grok/rules/bot3-deploy-policy.md`, `AGENTS.md`, `CLAUDE.md`.
